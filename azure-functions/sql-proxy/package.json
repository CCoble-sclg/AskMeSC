{
  "name": "askmesc-sql-proxy",
  "version": "1.0.0",
  "description": "Azure Function proxy for SQL database queries",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "mssql": "^10.0.0"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.9",
    "@types/node": "^20.0.0",
    "azure-functions-core-tools": "^4.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  }
}
